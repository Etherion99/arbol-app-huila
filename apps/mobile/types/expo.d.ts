// Tipos globales de Expo: importación de CSS, assets y variables de entorno.
//
// Normalmente los aporta `expo-env.d.ts`, que el CLI genera al arrancar y que está
// en .gitignore. Este archivo versionado hace la misma referencia para que
// `tsc --noEmit` funcione en CI sin necesidad de arrancar Expo primero.
/// <reference types="expo/types" />
