# React Native y Metro no resuelven el árbol de enlaces simbólicos de pnpm:
# necesitan un node_modules plano. Sin esto, la app móvil no compila.
node-linker=hoisted

# Versiones fijas: pnpm add no debe escribir rangos con ^ ni ~.
save-exact=true
save-prefix=
